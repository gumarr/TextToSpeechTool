# python/requirements.txt
#
# Install with: pip install -r requirements.txt
# Recommended: use a virtual environment
#   python -m venv .venv && .venv/Scripts/activate (Windows)
#   python -m venv .venv && source .venv/bin/activate (macOS/Linux)

# ── Web framework ──────────────────────────────────────────────────────────
fastapi>=0.109.0           # Modern async web framework
uvicorn[standard]>=0.27.0  # ASGI server — [standard] adds websocket + watchfiles

# ── PDF processing ────────────────────────────────────────────────────────
pymupdf>=1.23.0            # PyMuPDF — fast C-based PDF renderer (import as `fitz`)

# ── Text-to-speech ────────────────────────────────────────────────────────
edge-tts>=6.1.9            # Microsoft Edge TTS — free, no API key required

# ── Async file I/O ────────────────────────────────────────────────────────
aiofiles>=23.2.1           # async read/write for FastAPI async handlers

# ── Data validation (transitive — listed explicitly for clarity) ──────────
pydantic>=2.6.0            # FastAPI uses Pydantic v2 for request/response models
